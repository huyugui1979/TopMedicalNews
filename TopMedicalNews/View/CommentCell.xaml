<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:TopMedicalNews;assembly=TopMedicalNews" x:Class="TopMedicalNews.CommentCellXaml">
<ContentView.Resources>
		<ResourceDictionary>
			<local:DateTimeFormaterConverter x:Key="timeFormater">
			</local:DateTimeFormaterConverter>
		
		</ResourceDictionary>
	</ContentView.Resources>
		<StackLayout Orientation="Vertical">
			<StackLayout Orientation="Horizontal" >
				<Image Source="{Binding ImageUri}"  VerticalOptions="FillAndExpand" WidthRequest="20" />
				<StackLayout Orientation="Vertical" Spacing="0">
					<Label Text="{Binding UserName}" FontSize="12" TextColor="Blue" HorizontalOptions="StartAndExpand" />
					<Label Text="{Binding PublishTime,Converter={StaticResource timeFormater}}" FontSize="12" TextColor="#878787" HorizontalOptions="EndAndExpand" />
				</StackLayout>
			</StackLayout>
			<StackLayout Padding="20,0,0,0" Orientation="Horizontal" >
			<Label Text="{Binding Content}" FontSize="14" />
			</StackLayout>
		</StackLayout>
</ContentView>